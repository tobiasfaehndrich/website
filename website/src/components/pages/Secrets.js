import React from 'react';
import '../../App.css';
import background from '../../srcimages/clarertt.JPG'

export default function Secrets() {
    return <h1 className='secrets' style={{ backgroundImage: `url(${background})` }}>secret</h1>;
}
